# signals.md — Signal Types and Processing
# Stub — update as signal processing is built.
# Cover: signal types, processing logic, benchmark thresholds, pattern library status.
# The spec is in /prd/sections/10-signals.md and /prd/sections/11-warnings.md.

## Signal processing status
[Not yet built — signals table pending Supabase setup]

## Phase one scope reminder
Signal processing in phase one = logging and classification only.
Threshold evaluation and A/B test generation are phase two. See /docs/ADR.md ADR-011.

## Webhook endpoints (to be built)
- /api/webhooks/instantly
- /api/webhooks/lemlist
- /api/webhooks/ghl
